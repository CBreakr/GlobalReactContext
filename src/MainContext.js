import React from "react";

//
// just an arbitrary name, this can be anything
//
// here I'm setting a default value to be an empty object
//
const MainContext = React.createContext({});

export default MainContext;